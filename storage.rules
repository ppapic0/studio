rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /centers/{centerId}/attendance-request-proofs/{studentId}/{draftId}/{fileName} {
      allow write: if request.auth != null && request.auth.uid == studentId;
      allow read: if false;
    }
  }
}
