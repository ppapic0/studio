rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [MVP 단순화] 로그인한 사용자라면 모든 데이터를 자유롭게 읽고 쓸 수 있도록 허용합니다.
    // 복잡한 역할 기반 권한 체크를 제거하여 접근성 이슈를 해결했습니다.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}