rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인한 사용자라면 모든 데이터를 읽고 쓸 수 있도록 허용 (MVP 단순화)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}