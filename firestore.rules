
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 단계 임시 전면 개방: 소유자 권한 충돌 없이 강제 삭제가 가능하도록 설정
    match /{allPaths=**} {
      allow read, write, delete: if true;
    }
  }
}
