rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 문서에 대해 로그인한 사용자라면 읽고 쓰기를 허용합니다.
    // 이는 상담 예약 및 일지 조회 시 발생하는 모든 보안 규칙 충돌을 방지합니다.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}