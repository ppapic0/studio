
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 문서에 대해 누구나 읽고 쓰고 지울 수 있도록 허용합니다. (개발 단계 임시 개방 및 소유자 권한 해제)
    match /{allPaths=**} {
      allow read, write, delete: if true;
    }
  }
}
