
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 문서에 대해 누구나 읽고 쓰고 지울 수 있도록 허용합니다. (개발 단계 임시 전면 개방)
    // 데이터 삭제 시 소유자 권한 문제를 방지하기 위해 allow delete: if true를 적용합니다.
    match /{allPaths=**} {
      allow read, write, delete: if true;
    }
  }
}
